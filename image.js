<Image>
    //offline
    source ={ require('path')}
</Image>

<Image>
      //online
      source = {{uri = ""}}
      defaultSource = {require('path')}
      resizemode = cover -> all the spaces cover
                   contain -> adjust it self
                   strech -> cover full width and height
                   repeat -> repeat the image
                   center -> align the center

    // main Property overFlow ='hidden'
</Image> 